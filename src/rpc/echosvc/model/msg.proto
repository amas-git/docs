syntax = "proto3";
package model;

service Echo {
    rpc say(Msg) returns (Msg);
}

message Msg {
    int32 id = 1;
    string text = 2;
    enum Type {
        HIGH = 0;
        NORM = 1;
        LOW  = 2;
    }
    Type type = 3;
}